# Build artefacts and large regenerated outputs (see README)
*.exe
*.o
.Rhistory
.RData
.Rproj.user
*.tmp
~$*

# Posterior draws: regenerated by 1_MAKE_fit_models.R
data/posterior_*.RDS

# Sweep outputs: regenerated by 4_MAKE_si_validation_sweep.R (hours at full
# settings). The directory is kept so a fresh clone has somewhere to write.
si_output/*
!si_output/.gitkeep

*.log
